import React from "react";
import ReactDOM from "react-dom";
import styled from "styled-components";
const SDiv = styled.div`
  color: blue;
`;
const App = () => <SDiv>an app</SDiv>;
ReactDOM.render(<App />, document.getElementById("🚀"));
